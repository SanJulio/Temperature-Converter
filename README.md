Temperature Converter (Python)

This is a simple command-line temperature converter written in Python.

The program allows the user to convert temperatures between Celsius and Fahrenheit.  
It runs in a loop and continues until the user chooses to quit.
